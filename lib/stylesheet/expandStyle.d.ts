import { RawStyle, Style } from './types';
declare const _default: (style: RawStyle) => Style;
/**
 * Expand the shorthand properties to isolate every declaration from the others.
 */
export default _default;
